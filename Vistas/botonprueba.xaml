<UserControl x:Class="Vistas.botonprueba"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Button>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="64*" />
                <RowDefinition Height="236*" />
            </Grid.RowDefinitions>
            <TextBlock Height="44" HorizontalAlignment="Center" Name="textBlock1" Text="{Binding Content}" FontSize="30" VerticalAlignment="Center" Margin="102,20,102,0" />
            <Image Grid.Row="1" Name="image1" Source="/Vistas;component/Images/mesita.png" />
        </Grid>
    </Button>
</UserControl>
