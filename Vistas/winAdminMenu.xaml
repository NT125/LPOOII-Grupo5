<Window x:Class="Vistas.winAdminMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Menú de Administrador - NOMBRE DE LA EMPRESA" Height="464" Width="833" Background="#FF313131" MinWidth="833" MinHeight="464" WindowState="Normal" Topmost="False" WindowStartupLocation="CenterScreen" WindowStyle="SingleBorderWindow">
    <Grid>
        
        <!--menú superior-->
        <DockPanel Name="dpTopMenu" Height="34" HorizontalAlignment="Stretch" VerticalAlignment="Top" Width="Auto">
            <TextBlock Name="txtUserLabel" Text="Usuario:" FontFamily="FreeSans" FontSize="16" VerticalAlignment="Center" Foreground="Silver" Padding="10"/>
            <TextBlock Name="txtUserName" Text="username" FontFamily="FreeSans" FontSize="16" VerticalAlignment="Center" Foreground="Silver" Padding="5"/>
            <Button Name="btnLogout" Content="Cerrar Sesión" Height="34" HorizontalAlignment="Right" FontFamily="FreeSans" FontSize="13" VerticalAlignment="Top" Width="100" Background="#E1EF5050" BorderBrush="#001D0000" Foreground="White" ClipToBounds="True"/>
        </DockPanel>

        <!--Grid de menú principal-->
        <Grid HorizontalAlignment="Stretch" Margin="12,40,0,0" Name="mainGrid" VerticalAlignment="Stretch" Height="auto" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="251*" />
                <ColumnDefinition Width="536*" />
            </Grid.ColumnDefinitions>
            
            <!--Grid Botones-->
            <Grid Grid.ColumnSpan="1" Height="Auto" HorizontalAlignment="Stretch" Name="grid1" VerticalAlignment="Stretch" Width="Auto">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Button Content="Gestionar Usuarios" Grid.Row="0" Height="Auto" HorizontalAlignment="Stretch" Margin="10" Name="btnUsuarios" VerticalAlignment="Stretch" Width="Auto"/>
                <Button Content="Gestionar Artículos" Grid.Row="1" Height="Auto" HorizontalAlignment="Stretch" Margin="10" Name="btnArticulos" VerticalAlignment="Stretch" Width="Auto" Click="btnArticulos_Click" />
                <Button Content="Gestionar Familias" Grid.Row="2" Height="Auto" HorizontalAlignment="Stretch" Margin="10" Name="btnFamilia" VerticalAlignment="Stretch" Width="Auto" />
                <Button Content="Gestionar Categorías" Grid.Row="3" Height="Auto" HorizontalAlignment="Stretch" Margin="10" Name="btnCategoria" VerticalAlignment="Stretch" Width="Auto" />
                <Button Content="Gestionar Unidades de Medida" Grid.Row="4" Height="Auto" HorizontalAlignment="Stretch" Margin="10" Name="btnMedidas" VerticalAlignment="Stretch" Width="Auto" />
            </Grid>
        </Grid>
        <!--hora-->
        <Label Content="Label" Height="28" HorizontalAlignment="Right" VerticalAlignment="Bottom" FontFamily="FreeSans" FontSize="12" Name="lblTime" FlowDirection="LeftToRight" Width="42" IsEnabled="True" ForceCursor="True" ClipToBounds="True" Foreground="#FFEBEBEB" />        
    </Grid>
</Window>
